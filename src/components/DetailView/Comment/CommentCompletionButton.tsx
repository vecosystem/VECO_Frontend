/**
 * CommentCompletionButton.tsx
 * 상세페이지 댓글창) 댓글 입력 확인 버튼 컴포넌트
 *
 * @todo
 * - 댓글 입력 전에는 비활성화 상태(회색) 버튼으로 구현, 커서 중지 모양으로
 * - 댓글 입력하면 활성화 상태(네이비) 버튼으로 구현, 커서 손가락 모양으로
 */

const CommentCompletionButton = () => {
  return <button>확인</button>;
};

export default CommentCompletionButton;
